# React Todo App

This project was made for WDD3400 using React

## Available Scripts

In the project directory, you can run:
### `npm install`
to install node_modules then run:

### `npm start`

Open [http://localhost:3000](http://localhost:3000) to view it in the browser.


